{% use 'bootstrap_3_layout.html.twig' %}

{#
    La idea de este tema de formularios es manejar el estilo de bootstrap y
    para la apertura de formularios desactivamos las validaciones html5
#}
{%- block form_start -%}
    {% set attr = attr|merge({'novalidate':'novalidate'}) %}
    {{ parent() }}
{%- endblock -%}